name = "naturisa-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV namespace "naturisa-data" — reemplaza el id con el que aparece en el dashboard de Cloudflare
# Workers & Pages → KV → naturisa-data → copiar el ID
[[kv_namespaces]]
binding = "data"
id      = "f653f9c2841c4c8fa4be539e14c077d8"
